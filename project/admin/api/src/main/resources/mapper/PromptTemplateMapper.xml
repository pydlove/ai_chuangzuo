<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aichuangzuo.admin.modules.generation.mapper.PromptTemplateMapper">

    <select id="selectEnabled" resultType="com.aichuangzuo.shared.entity.PromptTemplate">
        SELECT id, name, base_content, user_style_guidance, system_prompt_json,
               enabled, remark, tenant_id, is_deleted,
               created_at, updated_at, created_by, updated_by
        FROM t_prompt_template
        WHERE enabled = 1
          AND is_deleted = 0
    </select>

</mapper>
