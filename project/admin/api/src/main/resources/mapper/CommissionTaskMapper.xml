<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aichuangzuo.admin.modules.commission.mapper.CommissionTaskMapper">

    <insert id="batchInsert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO u_commission_task (
            task_no,
            title,
            description,
            min_word_count,
            max_word_count,
            skill_hint,
            reward_coin,
            needed_count,
            adopted_count,
            status,
            deadline_at,
            selection_deadline_at,
            published_by,
            completed_at,
            tenant_id,
            is_deleted,
            created_by,
            updated_by
        ) VALUES
        <foreach collection="list" item="item" separator=",">
            (
                #{item.taskNo},
                #{item.title},
                #{item.description},
                #{item.minWordCount},
                #{item.maxWordCount},
                #{item.skillHint},
                #{item.rewardCoin},
                #{item.neededCount},
                #{item.adoptedCount},
                #{item.status},
                #{item.deadlineAt},
                #{item.selectionDeadlineAt},
                #{item.publishedBy},
                #{item.completedAt},
                #{item.tenantId},
                #{item.isDeleted},
                #{item.createdBy},
                #{item.updatedBy}
            )
        </foreach>
    </insert>

</mapper>
